# Expo 앱 환경변수. EXPO_PUBLIC_ 접두사는 클라이언트 번들에 그대로 포함되므로
# 비밀 값(시크릿 키 등)은 절대 넣지 않는다.

# 시뮬레이터/웹에서 로컬 서버
EXPO_PUBLIC_API_URL=http://localhost:8080

# 실기기에서는 개발 PC 의 LAN IP 를 넣는다
# EXPO_PUBLIC_API_URL=http://192.168.0.10:8080
